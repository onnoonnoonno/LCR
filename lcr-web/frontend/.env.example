# Backend API base URL
# In development, Vite proxies /api calls so this is only needed for production builds.
# In production, point this at your backend's public URL.
VITE_API_BASE_URL=http://localhost:3001
